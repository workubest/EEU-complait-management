// Mock data removed - application now uses real backend data only
// This file is kept for type imports but contains no mock data

import { Complaint, Customer } from '../types/complaint';
import { User } from '../types/user';

// All mock data has been removed to ensure the application works only with real backend data
export const mockCustomers: Customer[] = [];
export const mockComplaints: Complaint[] = [];
export const mockUsers: User[] = [];